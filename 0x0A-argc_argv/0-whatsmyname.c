#include <stdio.h>

/**
 * main - prints the name of of the program
 * @argc: array count
 * @argv: vector containing strings
 * Return: 0 if success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
