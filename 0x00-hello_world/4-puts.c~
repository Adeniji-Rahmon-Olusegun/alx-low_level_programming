#include <stdio.h>
/**
 * simply outputs a one-liner
 * main - This contains the 'put' function
 * Return: 0 if execution is successfull
 */

int main(void)
{
  put("\"Programming is like building a multilingual puzzle");

  return (0);
}
