#include <stdio.h>
/**
 * main - prints a one line using the ptrinf statement
 * Return: 0 if execution is successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
