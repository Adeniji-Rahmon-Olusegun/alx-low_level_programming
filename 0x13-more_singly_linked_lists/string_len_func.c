#include "lists.h"

/**
 * string_len_func - returns length of string
 *
 * @str: string
 *
 * Return: length of string
 */

size_t string_len_func(char *str)
{
	size_t str_len, index;

	str_len = 0;

	for (index = 0, str[index] != '\0', index++)
	{
		len++;
	}

	return (str_len);
}
